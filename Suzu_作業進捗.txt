//wheel0 -> 3番
//wheel1 -> 4番
//wheel2 -> 2番
//wheel3 -> 1番

/*
	行ったこと
	・モータ、センサの番号の確認
	・移動のプログラムの作成
	・速度を調整するプログラムの作成

	・移動について
		プログラム5参照

	・速度の調整方法
		　ループの回数によって調整を行う。
		　周期となる回数を定め、その間で、モータを動かす回数と停止させる回数を
		調整することで速度を調整する。
*/

/*
	今後、やること
	・ライントレースの方法の確立
	・モータごとに速度の制御が行えるようにする
	・オブジェクト化して、生の値を扱わなくてもよいようにする
	・方向を表す型の追加
*/

/*
	12 / 09 
	・行ったこと
		ライントレースの確認 (失敗)

	・次やること
		モータの番号の確認

	12 / 16
	・行ったこと
		モータの番号の確認 
			　結果、問題ないことが分かった。プログラムの回転方向を間違えてい
			たようである。ついでに全方向移動できるようにしといた。
		
		ライントレース
			　ラインが白の場合でトレース成功。直進の場合軌道修正は斜め移動の
			方が早そうだけど、カーブがある場合は旋回の方がよさそう。
		
		NXCのモータを動かす
			　nctrb_mc_io_writePortOUT1500() の引数の0～7bitでオムニ以外のモー
			タ用のポートを制御できる。早くここのクラスか関数を作っておきたい

	・次やること
		・モータごとにオブジェクトに、モータごとに速度の調整を行えるようにする。
		・黒ラインでのライントレースおよび、白ラインからの切り替え。
		・タッチセンサの実用化
		・C_WHEEL :: Do() の完成

*/
